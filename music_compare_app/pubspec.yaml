name: music_compare_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_card_swiper: ^2.0.4
  flutter_appauth: ^8.0.3
  http: ^1.3.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.0.20
  card_swiper: ^2.0.4
  uni_links: ^0.5.1  # Add this line
  flip_card: ^0.7.0
  # Add any other dependencies you need here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    # Add any other assets you need here

  # Uncomment and add custom fonts if you are using any
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700